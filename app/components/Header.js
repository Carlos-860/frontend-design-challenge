import { inter } from "../fonts";

export default function Header() {
    return (
        <header className={`${inter.className} h-[80px] bg-[#D9D9D9] grid place-items-center font-bold text-[28px]`}>HEADER</header>
    )
}